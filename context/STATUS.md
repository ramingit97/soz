# Söz — текущее состояние проекта

Обновлено: 2026-09-08. Этот файл — точка входа, если нужно вспомнить, где мы.
Пошаговая инструкция деплоя — в `RELEASE.md`. Стек — в `README.md`.

---

## Где мы сейчас

**Код готов к релизу.** Четыре спринта и предрелизная чистка закрыты:
80 тестов, типы чистые во всех 4 пакетах, Docker-образ собирается и стартует.

**Инфраструктура поднята (2026-09-08).** API живёт на `https://soz-api.fly.dev`,
почта проверена живым письмом, APK собран и готов к установке. Приложение в Play
Console создано (`az.soz.app`), анкеты ещё не заполнены.

**Следующий шаг — единственный, который сейчас блокирует прогресс:** поставить
APK на телефон и пройти чек-лист `RELEASE.md` §7. До device QA неизвестно, что
работает.

Ссылка на установку:
`https://expo.dev/accounts/ramin97leo/projects/soz/builds/522b13d7-e62a-4464-a008-76e3cbfd5d63`

Подробности сессии, три найденных бага и памятка по переезду на другой
компьютер — в `SESSION-2026-09-08-handoff.md`.

---

## Принятые решения (не пересматривать без причины)

| Вопрос | Решение | Где в коде |
|---|---|---|
| API | `https://soz-api.fly.dev`, одна машина `fra`, **выше 1 не поднимать** | `fly.toml`, лимитер в памяти |
| EAS-проект | `@ramin97leo/soz` · `4fc384df-3cbf-4fa1-a72e-59604dc8c10b` | `app.config.ts` (значением, не из env) |
| Git | `github.com/ramingit97/soz`, ветка `main`. **Репозиторий публичный** — владелец сделает приватным позже | — |
| Продажи через Play | **Работают из Азербайджана**, физлицо, юрлицо в другой стране НЕ нужно. Аккаунт продавца создан 2026-09-08 на профиле `RAMIN HASANOV` (5296-7551-5643, Yevlakh). Страна профиля не меняется никогда | Play Console → Управление заказами |
| Имя пакета | **`az.soz.app`** — зафиксировано в Play 2026-09-08, изменению не подлежит | `app.config.ts:22,41` |
| Название в сторе | На телефоне `Söz`; в листинге `Söz — язык с Хани` (меняется свободно) | `app.config.ts:5`, `STORE_METADATA.md` |
| Тип аккаунта | Физлицо, **Рамин Гасанов** (en: Ramin Hasanov, az: Ramin Həsənov) | `legal-site/`, `context/STORE_METADATA.md` |
| Почта приложения | `ramin.web.97@gmail.com` | `eas.json`, legal-страницы, `RESEND_REPLY_TO` |
| Домен | **Ещё нет и до продакшена не нужен.** Единственное назначение — отправка почты: Resend не шлёт с gmail. До релиза хватает тестового отправителя `onboarding@resend.dev` (доставляет только на свой же адрес). Политика — на GitHub Pages, домен не требуется. Плейсхолдер `YOUR-DOMAIN` в 11 местах | `grep -rn YOUR-DOMAIN` |
| Регион Neon | **us-east-1, не совпадает с Fly (fra)** — ~90–100 мс на каждый запрос к БД. Осознанно отложено 2026-09-08 как оптимизация; Neon не меняет регион, только новый проект | `fly.toml:11` |
| Модель Хани | `gpt-5.6-terra`, классификаторы `gpt-4o-mini`, генерация уроков `gpt-4o` | `apps/api/src/ai/models.ts`, env-override |
| Хранение транскриптов | **90 дней** после последней реплики, ежедневная чистка | `services/retention.ts`, политика обновлена |
| Кризисные алерты | Родителю (нейтрально, без цитаты) **и** оператору (с категорией), всегда | `services/safetyAlerts.ts`, `SAFETY_OPERATOR_EMAIL` обязателен |
| Триал | Настоящие анонимные аккаунты, 7 дней бесплатно, дальше пейволл | `/auth/guest`, `FREE_DAYS = 7` |
| Лимит разговоров | 30/день free, 600/день premium; родитель видит счётчик | `/talk/quota`, `parent.tsx` |
| Redis | Отложен. Лимитер в памяти → `min_machines_running = 1`, выше нельзя | `fly.toml` |
| Бюджет AI | `AI_DAILY_BUDGET_USD` — мягкий алерт, вызовы не блокирует. На тест ставить $3 | `services/spend.ts`, таблица `ai_spend_daily` |

---

## Что осталось — всё на стороне аккаунтов, не кода

Порядок по критическому пути:

1. **Device QA** — поставить APK, пройти чек-лист `RELEASE.md` §7. Блокирует всё
   остальное: пока не пройден, неизвестно, что чинить.
2. **Play Console → «Контент приложения»** — целевая аудитория (включает детей →
   анкета Families), Data safety, возрастной рейтинг. Тексты в `STORE_METADATA.md`.
3. **Публичный URL политики** — положить `legal-site/` на GitHub Pages. Домен
   для этого не нужен.
4. **RevenueCat** — без `EXPO_PUBLIC_RC_ANDROID_KEY` пейволл не появляется вообще,
   `initPurchases()` молча ничего не делает. Нужен, чтобы проверить покупку.
5. **Сервис-аккаунт Play** для `eas submit` (можно и вручную заливать .aab).
6. **Домен** (~$10/год) — перед закрытым тестом: без него тестировщики не получат
   ни восстановление пароля, ни кризисный алерт.
7. **Тестировщики** — Google требует 12 человек с установленным приложением
   14 дней подряд (считает установку, не активность). Собирать 15–20 с Android
   и их gmail. Не удалять две недели.

Сделано: Neon, Resend, Fly, Expo/EAS, сборка APK.

---

## Бюджет до релиза (замерено на реальном пайплайне)

Один ход ребёнка ≈ $0.0013 (LLM + STT + классификатор) + ~40 кредитов ElevenLabs.
Голос — единственная существенная статья.

| Сценарий | Сумма |
|---|---|
| 15 тестировщиков, умеренно (3–5 активных детей) | **≈ $50–60** |
| 20 тестировщиков, все активно каждый день | ≈ $150–165 |

Разбивка: Play $25 · ElevenLabs $11–22 · OpenAI $10 · Fly $3.32/мес · остальное $0.
Активный ребёнок весь месяц ≈ $2.5. **Подписка $13/мес** (решено 2026-09-08,
обоснование в `STORE_METADATA.md`), годовая $99.99.

Что остаётся с одного подписчика, при комиссии 15% и налоге 10%:

| | $13/мес | $4.99/мес (было) |
|---|---|---|
| Заплатил родитель | $13.00 | $4.99 |
| Комиссия Google 15% | −$1.95 | −$0.75 |
| Пришло тебе | $11.05 | $4.24 |
| Налог 10% | −$1.11 | −$0.42 |
| ИИ + инфраструктура | −$2.50 | −$2.50 |
| **Осталось** | **≈ $7.45** | **≈ $1.32** |

Расходы на ИИ: 23% от поступления вместо 50%. Даже очень разговорчивый ребёнок
за $4–5 в месяц оставляет больше пяти долларов — при $4.99 он уносил всю маржу.

**Две вещи определяют, сходится ли экономика вообще:**

1. **Программа снижения комиссии Google** — 15% вместо 30% на первый миллион в
   год. Регистрация бесплатная, баннер в платёжном профиле. При 30% из $13
   осталось бы $6.24 вместо $7.45.
2. **Налоговый режим.** Уточнить у бухгалтера: 10% с оборота или с прибыли.
   С прибыли расходы ($2.5) вычитаются до налога — разница около $0.25 с
   подписчика в месяц. Расходы документируемые: OpenAI, ElevenLabs, Deepgram,
   Fly, Neon — все по инвойсам.

---

## Что Claude должен знать о владельце

- Юрлица нет, проект личный. Gemini Plus у владельца есть, но для приложения
  это ничего не даёт (потребительский план, не API).
- Возможно, после релиза появится ключ Claude — смена провайдера это один
  адаптер в `apps/api/src/ai/llm.ts`.
- Владелец предпочитает короткие ответы и пошаговые списки «что мне / что тебе».

---

## Как проверить, что всё живо

```bash
corepack pnpm typecheck && corepack pnpm test      # 4 пакета, 80 тестов
curl https://soz-api.fly.dev/health/ready          # {"status":"ready",...}
~/.fly/bin/flyctl status -a soz-api                # должна быть ОДНА машина
```

**Перед любой сборкой EAS** — прогнать бандлер локально, иначе 15 минут очереди
ради «Unknown error»:

```bash
cd apps/mobile && ../../node_modules/.bin/expo export --platform android
```

`typecheck` и `test` НЕ доказывают, что приложение соберётся: Metro в них не
участвует. Именно так пропустили поломку резолюции `shared-types`, лежавшую
с 24 августа (см. `SESSION-2026-09-08-handoff.md`).

Деплой API — только из корня репозитория:

```bash
~/.fly/bin/flyctl deploy -c apps/api/fly.toml --remote-only --ha=false
```
