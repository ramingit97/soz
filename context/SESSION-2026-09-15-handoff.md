# Söz — сессия 2026-09-15/16: маскот (B1), разговор (B4), уроки (B5)

> Читать вместе с `PLAN-REDESIGN-2026-09-13.md` (разделы C и F, батч B4) и
> `SESSION-2026-09-14-handoff.md` (ловушки 1–15).

---

## Состояние на конец сессии

Коммиты на `main` (пушит владелец): `8bd0fa9` — B1 и B4, `335a9d7` — B5.
Опубликованы через EAS Update, канал `preview`, Android, runtime `1a02f268…`,
собранный `dist` (`--skip-bundler`): группа `26cf1396-e982-4934-953f-c0aad92f48cc`
(B1 + B4) и `70435a03-a0e9-4dd2-aeb0-d1c0970a4a98` (B5). API не менялся, на Fly
ничего не выкладывалось.

**Сборку переустанавливать не нужно.** Отпечаток нативной части `1a02f268…` не
изменился, обновление получает та же сборка `f3eb38b0`:
`https://expo.dev/accounts/ramin97leo/projects/soz/builds/f3eb38b0-d234-4e8f-bf4f-52d72eb72e65`
(применяется со второго запуска приложения).

Первое действие следующей сессии: `git status` (в `apps/mobile/babel.config.js` не
должно быть `unstable_transformImportMeta`), затем спросить владельца, что показали
три раунда маскота, «Говорить» и уроки на телефоне.

Открытые вопросы владельцу:
- **Раунд 1, силуэт:** на экране проверки (5 нажатий по «Söz · 1.0.0» внизу
  «Профиля») медвежонок и робот узнаются на 24 и на 200?
- **Раунд 2, лица:** понятны ли семь настроений (радуется, спокоен, интересно,
  слушает, думает, грустит, спит)?
- **Раунд 3, цвета и движение:** моргание, «говорит», жесты в покое.
- Робот для подростков 11+ и взрослых — трактовка «если ребёнок — медведь, иначе
  робот» (пункт 9 уточнений); подтвердить, что так.
- «Говорить»: видны ли пузыри, полоска ошибки над микрофоном, микрофон в цвете
  питомца.
- Уроки: пройти день целиком (слова → грамматика → разговор). Видно ли, на каком
  шаге ребёнок; хватает ли «×», чтобы выйти; не мелко ли на его телефоне.
- Встроенные уроки для малышей теперь про медвежонка («I'm a friendly bear cub»).
  Это запасной контент, когда плана с сервера нет; подтвердить, что так и надо.

---

## B1 — маскот

| Файл | Что |
|---|---|
| `src/components/mascot/art.ts` (новый) | рисунок как данные, без RN: `petArt({ kind, mood, palette, variant, detail, talking })` → слои `body / head / eyes / mouth / over`, место головы, оси глаз и рта; `petArtToSvg` — строка SVG для теста и картинок |
| `src/components/mascot/art.test.mts` (новый) | 8 тестов: лицо в каждом сочетании, мелкий размер без градиентов, ссылки на градиенты, «говорит» меняет только рот, тело только у `full` |
| `src/theme/petPalette.ts` (новый) | 7 оттенков (`PET_HUES`, `PetHue`), палитра `light/body/deep/cheek/muzzle/glow`, `nearestPetHue` |
| `src/theme/accent.ts` | `ACCENTS: Record<PetHue, Accent>`, ближайший оттенок через `nearestPetHue` |
| `src/components/HBPet.tsx` | переписан: `kind` (по умолчанию `useCompanionKind()` из стора), `hue` из стора, настроения `happy / neutral / curious / listening / thinking / sad / sleepy`, `variant`, `talking`. Моргание и рот — отдельные SVG-слои под `Animated.View`, сжатие по вертикали вокруг своей оси; неподвижный питомец — одним SVG. Проп `eyes` и `chroma` удалены |
| `src/components/Bobo.tsx` | тонкий алиас: круг `accent.soft` + `HBPet`, своё покачивание убрано (двойное движение) |
| `app/dev/mascot.tsx` (новый) | экран проверки: медвежонок/робот, говорит, без движения, авто/голова/целиком, 7 цветов; секции «силуэт» (24–200), «настроения» (96), «цвета» (72) |
| `app/profile.tsx` | «Söz · версия» внизу, 5 нажатий за 3 с → `/dev/mascot` |
| `app/profile-select.tsx` | у каждого ребёнка свой персонаж по его возрасту |
| `app/lesson/quest,world,pretend.tsx` | запись → `listening`, ожидание ответа → `thinking` (квест в ожидании был «спит») |

Решения по ходу (не из плана):
- **Тело целиком — от 100 dp, а не от 64.** На картинках фигурка 64–96 — мелкое
  лицо над туловищем; в шапках и карточках голова читается лучше. `FULL_BODY_FROM`.
- **Персонаж — по профилю, а не по `useUIMode()`.** Родительская зона принудительно
  `teen`, но персонаж шестилетки там остаётся медвежонком.
- **Без движения по умолчанию меньше 48 dp** (пузыри чата, аватары) — `still ?? low`.
  В шапке «Говорить» `still={false}`, чтобы рот двигался.
- `ReactingPet.react('thinking')` из плана не добавлен: экраны передают `mood`.

Цвета: у шалфея щёки были жёлтыми, у розы сливались с шерстью — подобраны заново.

## B4 — разговор

| Файл | Что |
|---|---|
| `src/components/ScreenHeader.tsx` (новый, вместо `HBScreenHeader`) | «назад» (`backIcon`, `backLabel`), заголовок слева или своё `center`, `right`; сверху `insets.top` |
| `src/components/InlineBanner.tsx` (новый) | полоска `info / warning / danger / success` с иконкой, заголовком, действием и «×» |
| `src/components/TypingDots.tsx` (новый) | три точки по очереди; в микрофоне и в пузыре «думаю» |
| `src/utils/talkAlert.ts` | `talkFailureMessage(e, bot)` для полоски; `alertTalkFailure` остался для `world` и `pretend` (B5) |
| `src/components/MicButton.tsx` | цвет — `accent.bottom`, запись — ягодный, думает/говорит — `accent.soft`; Lucide `mic` / `volume-2`; `accessibilityLabel` |
| `src/components/ObjectiveChips.tsx` | белые чипы с рамкой, выполненная цель — `accent.soft` и `circle-check` |
| `app/talk.tsx` | `ScreenHeader` с персонажем (слушает / думает / говорит) и статусом, язык — `globe` + код; пузыри: персонаж белый с рамкой, ребёнок `accent.soft`, без «хвостиков»; «думаю» — `TypingDots`; жалоба — иконка `flag`; согласие, микрофон, сбой, лимит, «жалоба отправлена» — `InlineBanner`; празднование целей и урока — карточка `Celebration`; подсказка — пилюля с `lightbulb`. Логика записи, воспроизведения, урока не менялась. Осталось одно `Alert.alert` — подтверждение жалобы |
| `app/topics.tsx` | `ScreenHeader`, персонаж со строкой, поле темы 52 dp, 8 карточек с Lucide (`school, gamepad-2, clapperboard, music, volleyball, plane, utensils, graduation-cap`) |
| `app/memory.tsx` | фон приложения вместо фиолетового градиента, `ScreenHeader`, `HBCard`, категории — Lucide (`palette, users, paw-print, sun, sparkles, heart`), «спросит тебя» — карточка `accent.soft` |
| `app/listening.tsx` | `ScreenHeader` с «Мои фразы» справа, персонаж со строкой, кнопки с иконками, ответы с галочкой/крестиком, ошибка — `InlineBanner`, библиотека — `headphones` + `chevron-right` |
| `src/components/Icon.tsx` | +15 иконок |

`check-ui` после батча: `rawFontSize 99` (−6), `textAlignCenter 140` (−11),
`emojiOnlyText 55` (−6), `bobo 16` (−2), `alert 5` (−4), `paddingTopLarge 21` (−2);
базовая линия обновлена.

## B5 — уроки (сделано 2026-09-16, коммит `335a9d7`)

| Файл | Что |
|---|---|
| `src/components/LessonHeader.tsx` (новый) | шапка шага: «×» на главный, название с иконкой, полоса «шаг N из M» (сегменты до 8 шагов, дальше сплошная), место справа. Отступ сверху — от безопасной зоны |
| `src/utils/lessonModes.ts` (новый) + тест | названия и иконки игровых режимов на языке интерфейса; `kidModeForDay` вынесен в `lessonFlow` (день 0 и отрицательный больше не дают `undefined` в маршруте) |
| `src/data/bearLessons.ts` (новый) + тест | встроенные уроки про медвежонка: 24 замены фраз в английских, 17 в русских, 🤖 → 🐻. Применяется в `getLesson` только к встроенным урокам и только при персонаже-медвежонке (`setCompanionKindProvider` в `_layout`); план с сервера не трогается |
| `src/services/replyAudio.ts` (новый) | проигрывание ответа персонажа: временный файл, режим динамика, страховка 30 с. «Покажи мир» и ролевая игра ждали `didJustFinish` без ограничения и могли навсегда застрять в «говорит» |
| `app/lesson/quest.tsx` | фон приложения вместо ночного градиента, `HBPet`, карточка сцены и слова, полоска при выключенном микрофоне (раньше кнопка просто молчала) |
| `app/lesson/tpr.tsx` | команды движения на RU/AZ (были RU/EN по языку урока), `HBCard`, `HBButton` «Я сделал!» |
| `app/lesson/pretend.tsx`, `world.tsx` | шапка, карточки, ошибки и лимит — `InlineBanner`, статус персонажа (`listening` / `thinking`), задания «Покажи мир» переписаны без падежей у имени персонажа |
| `app/lesson/word-game.tsx` | появилась кнопка выхода, прокрутка, ответы белые с рамкой, галочка и крестик иконками; пустой раунд не держит на экране |
| `app/lesson/grammar.tsx` | шапка с прогрессом, плитки и варианты на токенах; плитки первого упражнения больше не ставятся `setState` во время рендера |
| `app/lesson/complete.tsx` | прокрутка (на 320×712 не влезало), язык интерфейса, иконки вместо эмодзи, вопрос про уведомления после первого дня (`canAskNotificationPermission`), «Завтра» из общего модуля режимов. Подпись награды показывается, только если она на языке ребёнка |
| `app/lesson/milestone.tsx` | фон приложения вместо пяти кислотных градиентов, число в акценте, персонаж, кнопки «Продолжить» и «Поделиться» |
| `app/read.tsx` | общая шапка вместо своей, стрелки иконками, дубль «1 / N» внутри карточки убран |
| `app/lesson/listen.tsx` | удалён (недостижим) |
| `src/theme/modeTokens.ts` | токен `confetti` (kid 24, teen 10) — празднование отличается без развилки `mode ===` |
| `src/theme/mode.ts` | `companionKindFor` — один источник правды для `HBPet` и уроков |

Решения по ходу:
- **Язык уроков — интерфейса, а не изучаемого.** Было `isRu = lang === 'ru'`, то есть
  азербайджанский ребёнок, который учит английский, видел инструкции по-английски.
- **Встроенные уроки пересказаны заменами фраз, а не регуляркой по слову**: у
  русского «робот» пять падежей и «роботский», у медвежонка другой род.
  Переписывать 30 дней × 2 языка заново не стали — это запасной контент.
- **«×» уводит на главный, а не назад:** назад из грамматики вело в игру со
  словами, которую ребёнок уже прошёл.

Не сделано: `lesson/quiz.tsx` (контрольная, не входила в B5), эмодзи в текстах
уроков и сказок (по плану остаются), `AdultHome`.

---

## Проверки

| Проверка | Результат |
|---|---|
| `corepack pnpm typecheck` | 4 пакета чистые |
| `corepack pnpm test` | shared-types 17, mobile 59 (+8), api 59 |
| `node scripts/check-ui.mjs --check` | ОК, 6 метрик снизились |
| `expo export --platform android --max-workers 2` | собирается, `.hbc` 8 260 542 байт |
| `soz-api.fly.dev` / `localhost:3000` в бандле | 1 / 0 |
| `fingerprint:generate --platform android` | `1a02f26809db28d65c05866f86f7c217a7b57f52` — совпадает с `f3eb38b0` |
| картинки рисунка (Chrome по `file://`) | 2 персонажа × 7 настроений × голова/целиком, размеры 24–200, 7 цветов |
| веб-превью (дев-сервер, 360×760) | `dev/mascot` (слои и моргание работают), `talk` kid и teen-урок (AZ), полоска согласия после нажатия микрофона, `topics`, `memory` (робот), `listening`, `home` |
| после B5: `corepack pnpm test` | shared-types 17, mobile 67 (+8), api 59 |
| после B5: `expo export --platform android` | 8 239 110 байт, API 1 / localhost 0, отпечаток `1a02f268…` |
| веб-превью уроков | квест (360 и 320 dp), движение (AZ), ролевая игра, «Покажи мир» (AZ), слова, грамматика, завершение (kid RU, teen AZ, 320 dp), серия, чтение (kid AZ и teen RU) |

**На устройстве не проверено ничего.** Не видели глазами: пузыри с живым
разговором (в превью нет сервера и микрофона), полоску сбоя и лимита, карточку
«все цели выполнены», `listening` с историей и вопросами, `memory` с фактами,
анимацию на Android (`Animated.View` над SVG — на вебе работает). По B5 не
проверены: правильный и неправильный ответ в игре со словами и грамматике,
сборка предложения из плиток, ответ персонажа в ролевой игре и «Покажи мир»
(нужен сервер), вопрос про уведомления на экране завершения (в браузере
разрешений нет — карточка не показывается).

---

## Ловушки сессии

16. **Память машины.** Параллельно работает другой проект владельца (`nova`: ~12
    NestJS `--watch` и две Java по 1 ГБ) — занято ~13 из 15,6 ГБ, своп в ходу.
    Во время дев-сервера и статического веб-экспорта Metro сессия Claude дважды
    обрывалась (фоновые задачи «не завершились до конца сессии»). Вероятнее всего,
    это нехватка памяти (журнал ядра не смотрел) — и, возможно, та же причина, что
    у ловушки 15. Что помогло: одна сборка Metro за раз,
    `--max-workers 2` и `NODE_OPTIONS=--max-old-space-size=1800` (упадёт сам Metro,
    а не всё); веб — дев-сервером (`expo start --web`), не `expo export --platform
    web` (тот собирает ещё и серверный рендер).
17. **`eas update --skip-bundler --platform android`** публикует уже собранный
    `dist` — второй прогон Metro не нужен. `dist` перед этим проверять грепом на
    адрес API (ловушка из `RELEASE.md` §7a).
18. **Headless Chrome снова открывает http** (ловушка 14), если запускать сам
    бинарь `/opt/google/chrome/chrome`, а не обёртку `google-chrome`: тогда
    `kill` действительно его останавливает. Скрипт — `shots.mjs` из приложения к
    прошлому handoff, плюс `eval` и `Page.handleJavaScriptDialog`.
19. **Снимок «всей страницы» через увеличение окна даёт пустой кадр** на экранах с
    `ScrollView` — прокручивать сам прокручиваемый `div` (`scrollTop`) и снимать
    по частям.
20. **`aria-label` микрофона — на языке обучения** (`Tap to talk` при английском),
    а не интерфейса: искать кнопку по нему.
21. Типы маршрутов (`.expo/types`) обновляются только при `expo start`: новый
    маршрут `/dev/mascot` в `router.push` пока через `as any`.
22. Правка `Icon.tsx` скриптом по подстроке `lucide-react-native/icons/` задела
    комментарий в шапке файла — искать строки `import … from`, а не подстроку.

23. **Служебные символы в команде.** Строка `'\u0001'` в коде, который передаётся
    через heredoc, ломает вызов Bash-инструмента («control characters»). В таких
    местах собирать символ кодом (`String.fromCharCode(1)`) или писать файл
    инструментом записи.

---

## На потом

- B6 (родитель и пейволл), B7 (хвост: профиль, достижения, альбом, серия, сказка,
  песни, фразы, авторизация, оставшиеся `setup/*`).
- `lesson/quiz.tsx` (контрольная после 30 дней) — не входила в B5, осталась на
  старом виде.
- `app/talk.tsx` может перейти на общий `services/replyAudio.ts` (там та же
  логика воспроизведения своими руками).
- В `HomeHero` на вебе предупреждение «`<button>` внутри `<button>`» (было до этой
  сессии, на Android не влияет).
- Из прошлой сессии без изменений: логирование проглоченных ошибок генерации плана
  (ждёт согласия), `AdultHome`, текст запроса микрофона с «Хани» в `app.config.ts`
  (нативный), лимит `/auth/guest`.
